{
  "name": "java-consumer-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "scripts": {
    "build": "mbt build -t gen --mtar mtar.tar",
    "deploy": "cf deploy gen/mtar.tar",
    "run:local": "mvn spring-boot:run -Dspring-boot.run.profiles=default,mocked",
    "run:hybrid": "cds bind --exec -- mvn spring-boot:run   -Dspring-boot.run.profiles=default,hybrid"
  },
  "cds": {
    "requires": {
      "CatalogService": {
        "kind": "odata",
        "model": "srv/external/CatalogService"
      }
    }
  },
  "devDependencies": {
    "@sap/cds-dk": "^7"
  }
}
